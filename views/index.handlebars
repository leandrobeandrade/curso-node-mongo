<div class="jumbotron">
  <h1 class="display-4">Bem vindo ao Tecnoblog!</h1>
  <p class="lead">Fique sempre conectado a tudo que for de novo em tecnologia.</p>
  <hr class="my-4">
  <p>Crie, edite, exclua postagens da maneira que quiser.</p>
  <a class="btn btn-primary btn-lg" href="/usuarios/registro" role="button">Criar um conta</a>
</div>

<h2 class="text-center">Postagens Recentes</h2>

<div class="content">
    {{#each postagens}}
    <div class="card mt-4 mb-5 index">
        <div class="card-body">
            <h3 class="text-center">{{titulo}}</h3>
            <hr>
            <p>{{descricao}}</p>
            <small>Categoria: <b>{{categoria.nome}}</b></small>
        </div>
        <div class="card-footer">
            <a href="/postagem/{{slug}}"><button class="btn btn-primary btn-sm float-right">Ler post</button></a>
        </div>
    </div>
    {{else}}
    <h5>Não existem postagens</h5>
    {{/each}}
</div>
<br><br>

<style>
    .content {
        display: flex;
        flex-direction: row;
        flex-wrap: wrap;
        justify-content: space-around;
    }

    .index {
        width: 40%;
    }
    
    @media (max-width: 640px) {
        .content {
            display: flex;
            flex-direction: column;
        }
        .index {
            width: 100%;
        }
    }
</style>